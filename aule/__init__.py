"""Aule: patterned OSS stimulation from microscope images.

Workflow: calibrate camera -> DMD mapping once, draw shapes on a microscope image,
export a pattern set (masks), merge the masks as pos/neg frames into any bin file.
"""

__version__ = "0.1.0"
